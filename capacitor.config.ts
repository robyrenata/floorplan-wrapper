import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'jublia-floorplan-wrapper',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
